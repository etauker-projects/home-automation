// import { IPersistenceConfig, IPool, PersistenceConnector, PoolFactory } from '@etauker/connector-postgres';
// import { PersistenceConnector as Connector } from '@etauker/connector-postgres';
// import mysql from 'mysql2/promise';
// import { PersistenceTransaction } from './persistence.transaction';
// // import type { ConnectionPool } from './connection-pool';
// import type { IPersistenceConfig } from './persistence-config.interface';

// export interface IQueryConfig {
//     commit: boolean;
//     maxStatements: 1;
// }

export class PersistenceConnector {
    // private pool;
    // private config;
    // constructor(pool: IPool);
    // private connectionPromise: Promise<mysql.Connection>;
    // private pool: mysql.Pool;

    // constructor(config: IPersistenceConfig) {
    //     this.pool = mysql.createPool(config);
    // }

    // /**
    //  * Start a new database transaction.
    //  * Uses a connection from the pool assigned to the service instance.
    //  */
    // transact(): PersistenceTransaction {
    //     return new PersistenceTransaction(this.pool.getConnection());
    // }
}
